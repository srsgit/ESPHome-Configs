esphome:
  name: esphome-web-fb1430
  friendly_name: ttgo213

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:


wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esphome-Web-Fb1430"
    password: "pyZU3sBORfKp"

captive_portal:
    